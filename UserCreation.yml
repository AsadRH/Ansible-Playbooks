---
 - name: Create a login user
     user:
      name: ansible
      password: 'tpstps'
      groups:
       -sudo
      state: present
      shell: /bin/bash
      system: no
      createhome: yes
      home: /home/ansible
